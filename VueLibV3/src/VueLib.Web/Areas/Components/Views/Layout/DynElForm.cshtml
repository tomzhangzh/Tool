@{
    // DynElForm —— 表单容器组件（可拖拽子组件）
}
<el-form :model="model" :label-width="node.options.comoptions.labelWidth || '100px'"
    :style="node.options.itemoptions.style" :class="node.options.itemoptions.class"
    v-bind="node.options.comoptions">
    <slot></slot>
</el-form>
