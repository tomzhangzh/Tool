@{ Layout = "~/Areas/Component/Views/Common/_ContainerLayout.cshtml"; }

<template>
<el-container v-bind="jc.options.comoptions" v-on="getComlisteners()"
                  :style="getStyle(jc.options.itemoptions)"
                  :class="[...getClass(jc.options.itemoptions), designRootClass]">
        <div class="dyn-children-wrap"><n-dynamic-com v-for="(child, idx) in safeChildren"
                       :key="child.component + '_' + idx"
                       :jsonconfig="child"
                       :parentmodelinfo="props.parentmodelinfo"
                       :node-path="props.nodePath + '.childrenctrls[' + idx + ']'">
        </n-dynamic-com></div>
    </el-container>
</template>

@section setupScripts{ DVALUE = null; }
